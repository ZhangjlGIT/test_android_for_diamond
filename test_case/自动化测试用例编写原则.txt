在编写自动化测试用例过程中应该遵循以下原则：

1、一个用例为一个完整的场景，从用户登录系统到退出关闭浏览器

2、一个用例只验证一个功能点
（不要试图在用户登录系统后把所有的功能都验证一遍）

3、尽可能少的编写逆向逻辑用例（反用例）。
一方面因为逆向逻辑的用例很多（例如，手机号输入错误有几十种情况）
另一方面自动化脚本比较脆弱，复杂的逆向逻辑用例实现起来比较麻烦，容易出错。

4、用例与用例之间 尽量避免产生依赖

5、一条用例完成测试后，需要对测试场景进行还原，以避免影响其他用例的执行。